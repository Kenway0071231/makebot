#!/bin/bash

echo "🚀 Запуск MakeBot сервера v2.2.0..."
echo "📧 Проверка настроек почты..."

# Проверка переменных окружения
if [ -z "$SMTP_USER" ] || [ -z "$SMTP_PASS" ] || [ -z "$ADMIN_EMAIL" ]; then
    echo "⚠️  Внимание: SMTP настройки не установлены!"
    echo "   Письма не будут отправляться"
    echo "   Установите переменные в файле .env"
fi

echo "✅ Настройки проверены"
echo "📊 Порт сервера: $PORT"
echo "🌐 Режим работы: $NODE_ENV"

# Запуск сервера
exec node server.js
