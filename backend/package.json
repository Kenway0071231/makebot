{
  "name": "makebot-backend",
  "version": "2.2.0",
  "description": "Backend для сайта MakeBot - только Email уведомления",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup": "mkdir -p data && echo '[]' > data/calculator_requests.json && echo '[]' > data/contact_requests.json"
  },
  "keywords": ["chatbot", "ai", "website", "development", "email"],
  "author": "MakeBot Team <support@makebot.shop>",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
